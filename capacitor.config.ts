import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
	appId: 'com.mememaster.app',
	appName: 'Meme Master',
	webDir: './build/prerendered'
};

export default config;
